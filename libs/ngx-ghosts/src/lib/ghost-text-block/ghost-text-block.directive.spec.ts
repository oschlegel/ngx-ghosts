import { GhostTextBlockDirective } from './ghost-text-block.directive';

describe('TextBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new GhostTextBlockDirective();
    expect(directive).toBeTruthy();
  });
});
