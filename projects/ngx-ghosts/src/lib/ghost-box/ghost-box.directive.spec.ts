import { GhostBoxDirective } from './ghost-box.directive';

describe('GhostBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new GhostBoxDirective();
    expect(directive).toBeTruthy();
  });
});
